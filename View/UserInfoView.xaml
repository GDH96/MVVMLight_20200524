<Window x:Class="MVVMLight_20200524.View.UserInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMLight_20200524.View"
        mc:Ignorable="d"
        Title="UserInfoView" Height="450" Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=UserInfo}">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10 10 0 0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="请输入姓名" FontSize="20"></TextBlock>
                <TextBox Text="{Binding UserInfo.UserName,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" Width="200" FontSize="20"></TextBox>
            </StackPanel>
            <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                <TextBlock Text="Hello  " FontSize="20" ></TextBlock>
                <TextBlock Text="{Binding UserInfo.UserName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay }"  Width="200" FontSize="20"></TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
